{
  "datasourceId": 1,
  "sqlTemplate": "SELECT * FROM default.not_exists_table WHERE id = :id",
  "paramSchema": {
    "id": { "default": 1 }
  },
  "responseMode": "PAGE",
  "responseConfig": {
    "maxPageSize": 100,
    "timeoutSec": 30
  },
  "updatedBy": "admin"
}
